<% layout("/layouts/boilerplate") %>

<!-- <h2>All Users</h2>

<ul>
  <% users.forEach(user => { %>
      <% if(currentUser && user._id.toString() !== currentUser._id.toString()){ %>
        <li>
          <a href="/chat/<%= user._id %>" class="user">
            <%= user.username %>
          </a>
        </li>
      <% } %>
  <% }) %>
</ul> -->

<h2 class="mt-3 mb-3">All Users</h2>

<div class="container">
  <div class="row">
    <% users.forEach(user => { %>
      <% if(currentUser && user._id.toString() !== currentUser._id.toString()){ %>
        
        <div class="col-10 mb-3">
          <div class="card p-3 d-flex flex-row justify-content-between align-items-center">
            <a href="/chat/<%= user._id %>" class="user-link">
              <%= user.username %>
            </a>
            <div class="status">
              <span class="online-dot d-none" id="dot-<%= user._id %>"></span>
            </div>
          </div>
        </div>

      <% } %>
    <% }) %>
  </div>
</div>

<script>
  const currentUserId = "<%= currentUser._id %>";
</script>

<% block('js') %>
<script src="/socket.io/socket.io.js"></script>
<script src="/js/contact.js"></script>

<% block('css') %>
<link rel="stylesheet" href="/css/contact.css">

