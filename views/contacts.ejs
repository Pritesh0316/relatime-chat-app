<!DOCTYPE html>
<html>
<head>
  <title>Contacts</title>
</head>
<body>

  <h2>All Users</h2>

  <ul>
    <% users.forEach(user => { %>
      <li>
        <a href="/chat/<%= user._id %>" class="user">
          <%= user.username %>
        </a>
      </li>
    <% }) %>
  </ul>

</body>
</html>